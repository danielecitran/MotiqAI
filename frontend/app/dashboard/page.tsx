import { PlaceholderPage } from "@/components/placeholder-page";

export default function DashboardPage() {
  return (
    <PlaceholderPage
      title="Dashboard"
      description="Das Dashboard wird angezeigt, sobald die Authentifizierung und das Backend angebunden sind."
      backLabel="Zurück zur Startseite"
    />
  );
}
